#include "Friend.h"
#include <vector>

using namespace std;

int find(int x, std::vector<int>& parents) {
   // your code
   if(parents[i] < )
}

int findCircleNum(std::vector<std::vector<int>>& M) {
    // your code
}
